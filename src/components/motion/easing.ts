/** Shared motion vocabulary — one easing, a few durations, so everything feels related. */
export const EASE = [0.22, 1, 0.36, 1] as const; // quintic ease-out
export const DUR = { fast: 0.5, base: 0.9, slow: 1.6 } as const;
